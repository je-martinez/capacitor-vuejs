import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capacitor_vuejs_app.example',
  appName: 'capacitor-vuejs',
  webDir: 'dist'
};

export default config;
